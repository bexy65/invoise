<?php
// Define project root
define('BASE_PATH', __DIR__);

// Composer autoload (optional, for helpers)
// if (file_exists(BASE_PATH . '/vendor/autoload.php')) {
//     require BASE_PATH . '/vendor/autoload.php';
// }

// Load config
$config = require BASE_PATH . '/config/app.php';
